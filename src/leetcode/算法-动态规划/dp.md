```js
特么好难

动态规划
  主要用于求极值，每一步的状态都是前一步推导而来
  每走一步都要保存一个不同状态的最优解 最终得出最优解 有点类似用空间换时间
  走迷宫例子(走的数字和最小)
      2   100
    1 10  2
      6   90
  公式(斐波那契例子):
    // 1. 定义状态: i表示n dp[i]表示结果
    // 2. 定义转移方程式: dp[i] = dp[i-1]+dp[i-2]
    // 3. 确定初始条件和遍历顺序
    // 4. 边界判断
    function fibByDp(n: number): number {
      const dp = [0, 1];
      for (let i = 2; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];
      }
      return dp[n];
    }

题型
 - 最小花费爬楼梯
 - 买卖股票
 - 打家劫舍
```
